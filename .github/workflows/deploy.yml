name: Deploy
on:
  push:
    branches: ["main"]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Log in to the Image Repository
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.DOCKER_REGISTRY }}
          username: ${{ secrets.DOCKER_REGISTRY_USERNAME }}
          password: ${{ secrets.DOCKER_REGISTRY_PASSWORD }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKER_REGISTRY }}/${{ github.repository_owner }}/wanbit:${{ github.ref_name }}
             ${{ secrets.DOCKER_REGISTRY }}/${{ github.repository_owner }}/wanbit:latest

      - name: Notificar para fazer o deploy
        run: >
          curl -X POST 
          -H "Authorization: Bearer ${{ secrets.DEPLOY_API_TOKEN }}" 
          "${{ secrets.DEPLOY_WEBHOOK_URL }}"
